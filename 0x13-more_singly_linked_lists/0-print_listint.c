#include <stdio.h>
#include "lists.h"


/**
 * print_listint - It print all the elemnts of a linked list
 *
 * @h: linked list of type listint_t to print
 *
 * Return : The number of nodes
 */

size_t print_listint(const listint_t *h)
{
	size_t count = 0;

	while (head != NULL
	{
		count++;
		head = head->next;
	}
	return (count);


}

