#include "main.h"

/**
 * _abs - computes for the absolute value of an integer
 * @n: the int to be check
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
